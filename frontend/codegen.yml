overwrite: true
schema: "http://localhost:3333/graphql"
documents: "src/**/*.gql"
generates:
  src/graphql/generated/:
    preset: "client"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
